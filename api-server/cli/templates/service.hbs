import { Inject, Service } from "@/core/container";
import { {{name}} } from "@/models/entity/{{name}}";
import { BaseService } from "@/service/baseService";
import { Repository } from 'typeorm';

@Service()
export class {{name}}Service extends BaseService {
  @Inject('{{name}}')
  public repository:Repository<{{name}}>
}