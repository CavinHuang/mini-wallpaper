//跳过验证
const optionAPI = { skipPerm: true }
const method = 'get'
const handle = async (raw,  ctx, routeFinal, $) => {
  const { logWarn } = $
  logWarn(1111)

  return {
    a: 1,
    b: 2
  }
}

export { method, handle, optionAPI }